version: '3.8'

services:
  web:
    build: .
    image: simple-diary:latest
    ports:
      - "3001:3001"
    environment:
      - PORT=3001
    volumes:
      - ./data:/usr/src/app/data
    restart: unless-stopped
